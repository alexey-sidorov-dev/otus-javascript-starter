/*
  1. В переменных a и b хранятся числа. Вывести в
  консоль наибольшее из них.
 */

export function printMaxNumber(firstNumber, secondNumber) {
  console.log(Math.max(firstNumber, secondNumber));
}
